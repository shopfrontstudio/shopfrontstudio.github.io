---
// Resting figure: an exact halftone pomegranate traced from the brand logo.
// Each dot's position, size and colour is sampled from the source artwork, so
// the count, spacing, ~12 degree clockwise tilt and inside-out colour match.
// Crown = radiating leaf petals. During animation JS moves each dot to a
// target; at rest they hold this figure.
const dots = [
  { x: 228.75, y: 194.07, r: 13.19, fill: "#ae4550" },
  { x: 193.12, y: 204.34, r: 13.25, fill: "#ac414c" },
  { x: 333.22, y: 197.94, r: 5.80, fill: "#a45e64" },
  { x: 369.90, y: 200.37, r: 6.87, fill: "#ae676b" },
  { x: 299.92, y: 208.62, r: 9.14, fill: "#b25c63" },
  { x: 258.60, y: 218.87, r: 12.32, fill: "#b2505a" },
  { x: 411.93, y: 216.40, r: 8.34, fill: "#ad666a" },
  { x: 217.98, y: 231.24, r: 13.14, fill: "#b24953" },
  { x: 360.49, y: 228.74, r: 11.10, fill: "#b7686e" },
  { x: 314.50, y: 239.90, r: 12.45, fill: "#b45861" },
  { x: 452.63, y: 239.03, r: 8.64, fill: "#a95c62" },
  { x: 176.90, y: 246.17, r: 12.22, fill: "#aa464f" },
  { x: 268.79, y: 252.54, r: 14.20, fill: "#b24d57" },
  { x: 406.07, y: 256.18, r: 14.25, fill: "#b75f67" },
  { x: 138.81, y: 262.71, r: 9.04, fill: "#a2474f" },
  { x: 220.85, y: 267.40, r: 14.40, fill: "#a93e4b" },
  { x: 355.99, y: 270.51, r: 16.50, fill: "#b3515c" },
  { x: 486.47, y: 274.54, r: 9.17, fill: "#a5575c" },
  { x: 303.77, y: 284.43, r: 18.14, fill: "#ad414e" },
  { x: 107.76, y: 279.39, r: 6.55, fill: "#944048" },
  { x: 175.41, y: 286.03, r: 13.68, fill: "#a33a46" },
  { x: 445.52, y: 288.43, r: 14.56, fill: "#b45a61" },
  { x: 250.09, y: 301.45, r: 19.31, fill: "#9d2e3f" },
  { x: 396.16, y: 305.34, r: 17.63, fill: "#aa4853" },
  { x: 135.18, y: 304.29, r: 11.83, fill: "#a03d47" },
  { x: 84.65, y: 307.12, r: 5.20, fill: "#893f45" },
  { x: 341.37, y: 322.33, r: 20.12, fill: "#973140" },
  { x: 512.07, y: 314.38, r: 9.06, fill: "#9a4d54" },
  { x: 197.18, y: 323.78, r: 17.96, fill: "#932939" },
  { x: 102.60, y: 323.43, r: 7.61, fill: "#993f46" },
  { x: 474.77, y: 329.33, r: 14.61, fill: "#a14c54" },
  { x: 286.33, y: 339.53, r: 20.91, fill: "#832234" },
  { x: 148.82, y: 344.70, r: 15.76, fill: "#962b3b" },
  { x: 429.32, y: 345.67, r: 17.70, fill: "#983845" },
  { x: 71.21, y: 343.36, r: 6.55, fill: "#913c43" },
  { x: 230.40, y: 361.16, r: 20.98, fill: "#721b2d" },
  { x: 377.31, y: 363.08, r: 20.62, fill: "#7d2434" },
  { x: 526.97, y: 355.24, r: 8.61, fill: "#8f444b" },
  { x: 105.48, y: 365.22, r: 13.44, fill: "#a33643" },
  { x: 493.11, y: 374.27, r: 14.65, fill: "#913f48" },
  { x: 322.57, y: 382.72, r: 21.84, fill: "#641829" },
  { x: 176.73, y: 384.22, r: 18.76, fill: "#832032" },
  { x: 66.80, y: 382.35, r: 8.11, fill: "#993c44" },
  { x: 448.97, y: 391.52, r: 17.88, fill: "#882d3b" },
  { x: 264.72, y: 403.41, r: 22.35, fill: "#571425" },
  { x: 131.58, y: 404.74, r: 15.14, fill: "#972a3a" },
  { x: 398.42, y: 410.32, r: 20.62, fill: "#6c1c2d" },
  { x: 535.00, y: 398.39, r: 7.73, fill: "#803841" },
  { x: 208.88, y: 426.85, r: 21.43, fill: "#631628" },
  { x: 503.02, y: 420.21, r: 12.98, fill: "#883843" },
  { x: 94.55, y: 420.96, r: 11.65, fill: "#a73c48" },
  { x: 343.16, y: 431.54, r: 21.97, fill: "#531223" },
  { x: 65.00, y: 423.61, r: 7.88, fill: "#9b3c45" },
  { x: 462.00, y: 437.97, r: 16.72, fill: "#7e2837" },
  { x: 158.11, y: 448.79, r: 18.37, fill: "#872233" },
  { x: 285.71, y: 453.38, r: 22.33, fill: "#521222" },
  { x: 534.24, y: 439.29, r: 6.59, fill: "#7b3840" },
  { x: 414.19, y: 458.84, r: 19.60, fill: "#651929" },
  { x: 113.22, y: 467.72, r: 14.98, fill: "#a93443" },
  { x: 505.29, y: 463.75, r: 9.92, fill: "#80353e" },
  { x: 72.14, y: 463.46, r: 8.48, fill: "#a03b45" },
  { x: 229.45, y: 475.97, r: 21.01, fill: "#641628" },
  { x: 361.65, y: 480.17, r: 20.95, fill: "#571324" },
  { x: 467.96, y: 483.71, r: 13.83, fill: "#792534" },
  { x: 526.96, y: 478.68, r: 4.78, fill: "#6e363b" },
  { x: 179.91, y: 495.90, r: 17.95, fill: "#8a2233" },
  { x: 306.75, y: 501.57, r: 20.78, fill: "#5a1426" },
  { x: 426.35, y: 504.54, r: 16.38, fill: "#6d1b2b" },
  { x: 86.01, y: 501.34, r: 7.94, fill: "#a03e47" },
  { x: 135.54, y: 513.21, r: 14.28, fill: "#a73442" },
  { x: 504.15, y: 506.96, r: 6.93, fill: "#762f38" },
  { x: 254.23, y: 522.21, r: 19.29, fill: "#721a2c" },
  { x: 379.08, y: 525.41, r: 18.15, fill: "#661828" },
  { x: 468.97, y: 525.23, r: 9.59, fill: "#772935" },
  { x: 205.70, y: 538.64, r: 16.41, fill: "#992a3b" },
  { x: 105.11, y: 535.58, r: 8.00, fill: "#a03f48" },
  { x: 328.92, y: 545.16, r: 18.51, fill: "#6b1a2b" },
  { x: 432.17, y: 546.45, r: 12.47, fill: "#792231" },
  { x: 491.63, y: 539.74, r: 4.53, fill: "#693138" },
  { x: 161.74, y: 552.06, r: 12.94, fill: "#a93a47" },
  { x: 278.71, y: 562.91, r: 17.01, fill: "#8c2435" },
  { x: 390.19, y: 566.07, r: 13.98, fill: "#802433" },
  { x: 461.83, y: 563.03, r: 5.84, fill: "#732e38" },
  { x: 129.33, y: 567.25, r: 7.20, fill: "#9d434b" },
  { x: 231.86, y: 576.81, r: 14.17, fill: "#a63644" },
  { x: 345.85, y: 583.87, r: 14.48, fill: "#8c2838" },
  { x: 190.48, y: 586.63, r: 10.93, fill: "#ac4650" },
  { x: 428.97, y: 584.89, r: 7.42, fill: "#812d39" },
  { x: 300.73, y: 597.81, r: 13.23, fill: "#a03443" },
  { x: 155.95, y: 594.24, r: 5.51, fill: "#9d4c52" },
  { x: 396.18, y: 600.64, r: 9.42, fill: "#8e313d" },
  { x: 257.19, y: 608.46, r: 10.82, fill: "#a8444d" },
  { x: 361.28, y: 614.42, r: 10.04, fill: "#963341" },
  { x: 188.34, y: 614.44, r: 4.68, fill: "#9c5357" },
  { x: 222.56, y: 622.02, r: 6.97, fill: "#a14850" },
  { x: 323.96, y: 624.39, r: 9.06, fill: "#a03f48" },
  { x: 286.23, y: 629.93, r: 6.59, fill: "#9b444d" },
];

const crownLeaves = [
  { d: "M 206.3 187.5 C 180.5 157.2 171.8 115.4 183.2 77.3 C 209.0 107.6 217.7 149.4 206.3 187.5 Z", fill: "#6c172b" },
  { d: "M 263.1 89.8 C 268.1 123.3 255.5 157.4 230.0 179.6 C 224.9 146.1 237.5 112.0 263.1 89.8 Z", fill: "#6c182c" },
  { d: "M 183.3 192.2 C 149.7 188.0 121.2 166.2 108.4 134.8 C 142.1 139.0 170.5 160.8 183.3 192.2 Z", fill: "#741b2e" },
  { d: "M 276.9 151.9 C 276.1 171.5 265.6 190.2 249.1 201.0 C 249.9 181.3 260.4 162.7 276.9 151.9 Z", fill: "#a13845" },
  { d: "M 182.3 224.5 C 159.3 226.4 137.5 215.9 124.7 196.7 C 147.7 194.8 169.5 205.3 182.3 224.5 Z", fill: "#a23442" },
];
---

<div class="anara-anim relative w-full max-w-[26rem] md:max-w-[30rem] lg:max-w-[34rem] mx-auto">
  <svg
    viewBox="0 0 600 760"
    class="w-full h-auto block drop-shadow-[0_30px_50px_rgba(185,29,58,0.30)]"
    preserveAspectRatio="xMidYMid meet"
    role="img"
    aria-label="Anara pomegranate mark"
  >
    {/* Radiating crown of leaf petals */}
    <g data-leaves style="transition: opacity 800ms ease;">
      {crownLeaves.map((l) => <path d={l.d} fill={l.fill} />)}
    </g>

    {/* Edges of the knowledge graph — populated by JS during cluster phases */}
    <g data-edges fill="none" stroke="rgba(243,237,224,0.32)" stroke-width="0.65" style="opacity: 0; transition: opacity 700ms ease;"></g>

    {/* The dot field — the body of the fruit at rest, the swarm during motion */}
    <g data-dots>
      {dots.map((d) => (
        <circle
          cx={d.x} cy={d.y} r={d.r}
          fill={d.fill}
          data-hx={d.x} data-hy={d.y} data-hr={d.r}
        />
      ))}
    </g>

    {/* One-word service punch shown during cluster phases */}
    <text
      data-word
      x="300" y="706"
      text-anchor="middle"
      style="font-family: 'Cormorant Garamond', Georgia, serif; font-size: 64px; font-weight: 500; letter-spacing: -0.01em; fill: #f3ede0; opacity: 0; transition: opacity 700ms ease;"
    >
      Found.
    </text>
  </svg>
</div>

<script is:inline>
(function () {
  const wrap = document.querySelector(".anara-anim");
  if (!wrap) return;
  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

  const SVG_NS = "http://www.w3.org/2000/svg";
  const svg = wrap.querySelector("svg");
  const dotEls = Array.from(svg.querySelectorAll("[data-dots] circle"));
  const wordEl = svg.querySelector("[data-word]");
  const leavesEl = svg.querySelector("[data-leaves]");
  const edgesG = svg.querySelector("[data-edges]");

  // Centre the motion on the fruit's core.
  const CX = 300, CY = 412;

  // Spring-physics state per dot
  const dots = dotEls.map((el, i) => ({
    el, idx: i,
    hx: +el.dataset.hx, hy: +el.dataset.hy, hr: +el.dataset.hr,
    x: +el.dataset.hx, y: +el.dataset.hy, r: +el.dataset.hr,
    vx: 0, vy: 0,
    tx: +el.dataset.hx, ty: +el.dataset.hy, tr: +el.dataset.hr,
    seed: Math.random(),
    seed2: Math.random(),
    pulsePhase: Math.random() * Math.PI * 2,
    isAnchor: false,
  }));
  const N = dots.length;

  // The knowledge-graph skeleton: 6 anchor nodes in a hexagon around the centre.
  const ANCHOR_COUNT = 6;
  let anchorPositions = [];

  function computeAnchors(phaseIndex) {
    const baseRot = phaseIndex * 0.18 * Math.PI * 2;
    const radius = 145;
    return Array.from({ length: ANCHOR_COUNT }, (_, i) => {
      const a = (i / ANCHOR_COUNT) * Math.PI * 2 + baseRot;
      return {
        x: CX + Math.cos(a) * radius,
        y: CY + Math.sin(a) * radius * 0.78,
      };
    });
  }

  // --- Target generators ---
  function setHome() {
    dots.forEach((d) => { d.tx = d.hx; d.ty = d.hy; d.tr = d.hr; d.isAnchor = false; });
    edgesG.style.opacity = "0";
  }

  function setScatter() {
    dots.forEach((d) => {
      const angle = d.seed * Math.PI * 2;
      const dist = 130 + d.seed2 * 200;
      d.tx = CX + Math.cos(angle) * dist;
      d.ty = CY + Math.sin(angle) * dist;
      d.tr = 2 + d.seed * 4;
      d.isAnchor = false;
    });
    edgesG.style.opacity = "0";
  }

  function setKnowledgeCluster(phaseIndex) {
    anchorPositions = computeAnchors(phaseIndex);
    const groupSize = Math.floor(N / ANCHOR_COUNT);

    dots.forEach((d, i) => {
      const anchorIdx = Math.min(Math.floor(i / groupSize), ANCHOR_COUNT - 1);
      const anchor = anchorPositions[anchorIdx];
      const isFirstInGroup = (i % groupSize) === 0;

      if (isFirstInGroup) {
        d.tx = anchor.x;
        d.ty = anchor.y;
        d.tr = 12 + d.seed * 3;
        d.isAnchor = true;
      } else {
        const orbitAngle = d.seed * Math.PI * 2 + phaseIndex * 0.6;
        const orbitR = 16 + d.seed2 * 32;
        d.tx = anchor.x + Math.cos(orbitAngle) * orbitR;
        d.ty = anchor.y + Math.sin(orbitAngle) * orbitR;
        d.tr = 1.8 + d.seed * 3;
        d.isAnchor = false;
      }
    });

    edgesG.innerHTML = "";
    for (let i = 0; i < anchorPositions.length; i++) {
      const a = anchorPositions[i];
      const next = anchorPositions[(i + 1) % anchorPositions.length];
      const e1 = document.createElementNS(SVG_NS, "line");
      e1.setAttribute("x1", a.x.toFixed(2));
      e1.setAttribute("y1", a.y.toFixed(2));
      e1.setAttribute("x2", next.x.toFixed(2));
      e1.setAttribute("y2", next.y.toFixed(2));
      edgesG.appendChild(e1);
      const diag = anchorPositions[(i + 2) % anchorPositions.length];
      const e2 = document.createElementNS(SVG_NS, "line");
      e2.setAttribute("x1", a.x.toFixed(2));
      e2.setAttribute("y1", a.y.toFixed(2));
      e2.setAttribute("x2", diag.x.toFixed(2));
      e2.setAttribute("y2", diag.y.toFixed(2));
      e2.setAttribute("stroke-opacity", "0.5");
      edgesG.appendChild(e2);
    }
    edgesG.style.opacity = "1";
  }

  const setLeaves = (a) => { if (leavesEl) leavesEl.style.opacity = String(a); };

  // The cycle — pomegranate at rest, disperse, three clusters, reform.
  const sequence = [
    { name: "rest",     duration: 3800, action: setHome,                       word: null,      leaves: 1.0 },
    { name: "disperse", duration: 1200, action: setScatter,                    word: null,      leaves: 0.18 },
    { name: "found",    duration: 2500, action: () => setKnowledgeCluster(0),  word: "Found.",  leaves: 0.10 },
    { name: "filled",   duration: 2500, action: () => setKnowledgeCluster(1),  word: "Filled.", leaves: 0.10 },
    { name: "kept",     duration: 2500, action: () => setKnowledgeCluster(2),  word: "Kept.",   leaves: 0.10 },
    { name: "reform",   duration: 1800, action: setHome,                       word: null,      leaves: 1.0 },
  ];

  let idx = 0;
  let phaseStart = performance.now();

  function applyPhase(p) {
    p.action();
    if (p.word) {
      wordEl.textContent = p.word;
      wordEl.style.opacity = "1";
    } else {
      wordEl.style.opacity = "0";
    }
    setLeaves(p.leaves);
  }
  applyPhase(sequence[0]);

  const STIFFNESS = 0.028;
  const DAMPING = 0.83;

  let raf = 0;
  let running = false;
  const startTime = performance.now();

  function tick(now) {
    if (!running) return;
    if (now - phaseStart > sequence[idx].duration) {
      idx = (idx + 1) % sequence.length;
      phaseStart = now;
      applyPhase(sequence[idx]);
    }

    const isRest = sequence[idx].name === "rest";
    const t = (now - startTime) * 0.001;

    for (let i = 0; i < dots.length; i++) {
      const d = dots[i];

      let tx = d.tx, ty = d.ty;
      if (isRest) {
        tx += Math.sin(t * 1.2 + d.seed * 6) * 0.6;
        ty += Math.cos(t * 1.5 + d.seed2 * 5) * 0.5;
      }

      d.vx = (d.vx + (tx - d.x) * STIFFNESS) * DAMPING;
      d.vy = (d.vy + (ty - d.y) * STIFFNESS) * DAMPING;
      d.x += d.vx;
      d.y += d.vy;

      d.r += (d.tr - d.r) * 0.09;

      let displayR = d.r;
      if (d.isAnchor) {
        displayR = d.r * (1 + Math.sin(t * 3.2 + d.pulsePhase) * 0.14);
      }

      d.el.setAttribute("cx", d.x.toFixed(2));
      d.el.setAttribute("cy", d.y.toFixed(2));
      d.el.setAttribute("r", displayR.toFixed(2));
    }

    raf = requestAnimationFrame(tick);
  }

  function start() {
    if (running) return;
    running = true;
    phaseStart = performance.now();
    raf = requestAnimationFrame(tick);
  }
  function stop() {
    if (!running) return;
    running = false;
    cancelAnimationFrame(raf);
  }

  const io = new IntersectionObserver(
    (entries) => entries.forEach((e) => (e.isIntersecting ? start() : stop())),
    { threshold: 0.05 }
  );
  io.observe(wrap);
})();
</script>


