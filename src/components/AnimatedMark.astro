---
// Resting figure: an exact halftone pomegranate traced from the brand logo.
// Each dot's position, size and colour is sampled from the source artwork, so
// the count, spacing, ~12 degree clockwise tilt and inside-out colour match.
// Crown = radiating leaf petals. During animation JS moves each dot to a
// target; at rest they hold this figure.
const dots = [
  { x: 262.98, y: 185.16, r: 15.31, fill: "#a13845" },
  { x: 228.75, y: 202.78, r: 13.16, fill: "#ae4550" },
  { x: 193.12, y: 213.03, r: 13.20, fill: "#ad414c" },
  { x: 333.27, y: 206.66, r: 5.72, fill: "#a76066" },
  { x: 369.94, y: 209.08, r: 6.87, fill: "#ae676b" },
  { x: 299.89, y: 217.34, r: 9.12, fill: "#b25c63" },
  { x: 258.62, y: 227.56, r: 12.30, fill: "#b3515a" },
  { x: 411.98, y: 225.13, r: 8.34, fill: "#ad666a" },
  { x: 217.94, y: 240.03, r: 13.11, fill: "#b24954" },
  { x: 360.59, y: 237.44, r: 11.06, fill: "#b8686f" },
  { x: 314.54, y: 248.67, r: 12.40, fill: "#b55861" },
  { x: 452.70, y: 247.76, r: 8.64, fill: "#a95c62" },
  { x: 176.91, y: 254.84, r: 12.19, fill: "#ab464f" },
  { x: 268.84, y: 261.31, r: 14.12, fill: "#b44d58" },
  { x: 406.16, y: 264.93, r: 14.20, fill: "#b86068" },
  { x: 138.77, y: 271.44, r: 8.99, fill: "#a4474f" },
  { x: 220.82, y: 276.14, r: 14.35, fill: "#a93f4b" },
  { x: 356.07, y: 279.29, r: 16.47, fill: "#b3515c" },
  { x: 486.58, y: 283.25, r: 9.14, fill: "#a5575c" },
  { x: 303.81, y: 293.16, r: 18.08, fill: "#ad424f" },
  { x: 107.68, y: 288.20, r: 6.52, fill: "#954149" },
  { x: 175.42, y: 294.79, r: 13.67, fill: "#a33a46" },
  { x: 445.65, y: 297.17, r: 14.53, fill: "#b45a62" },
  { x: 250.11, y: 310.27, r: 19.26, fill: "#9e2e3f" },
  { x: 396.21, y: 314.04, r: 17.57, fill: "#ab4954" },
  { x: 135.12, y: 313.09, r: 11.80, fill: "#a13d48" },
  { x: 84.63, y: 315.95, r: 5.16, fill: "#8a3f46" },
  { x: 341.44, y: 331.09, r: 20.09, fill: "#973140" },
  { x: 512.18, y: 323.12, r: 8.99, fill: "#9c4e55" },
  { x: 197.14, y: 332.52, r: 17.89, fill: "#942939" },
  { x: 102.55, y: 332.21, r: 7.55, fill: "#9b4047" },
  { x: 474.89, y: 338.02, r: 14.56, fill: "#a24c54" },
  { x: 286.35, y: 348.27, r: 20.90, fill: "#832234" },
  { x: 148.74, y: 353.51, r: 15.69, fill: "#972c3b" },
  { x: 429.43, y: 354.42, r: 17.65, fill: "#983945" },
  { x: 71.21, y: 352.08, r: 6.52, fill: "#923c44" },
  { x: 230.34, y: 369.92, r: 20.91, fill: "#731b2d" },
  { x: 377.40, y: 371.83, r: 20.56, fill: "#7e2434" },
  { x: 527.07, y: 364.12, r: 8.51, fill: "#92454c" },
  { x: 105.40, y: 373.99, r: 13.43, fill: "#a33643" },
  { x: 493.24, y: 383.00, r: 14.63, fill: "#913f48" },
  { x: 322.58, y: 391.52, r: 21.68, fill: "#651829" },
  { x: 176.63, y: 393.03, r: 18.69, fill: "#842032" },
  { x: 66.74, y: 391.13, r: 8.12, fill: "#993c44" },
  { x: 449.02, y: 400.31, r: 17.87, fill: "#882d3b" },
  { x: 264.72, y: 412.20, r: 22.26, fill: "#581425" },
  { x: 131.51, y: 413.51, r: 15.08, fill: "#982a3a" },
  { x: 398.51, y: 419.10, r: 20.59, fill: "#6d1c2d" },
  { x: 535.00, y: 407.23, r: 7.67, fill: "#813941" },
  { x: 208.93, y: 435.64, r: 21.32, fill: "#631628" },
  { x: 503.10, y: 429.00, r: 12.90, fill: "#893943" },
  { x: 94.50, y: 429.78, r: 11.56, fill: "#a93d49" },
  { x: 343.19, y: 440.35, r: 21.83, fill: "#531223" },
  { x: 65.00, y: 432.34, r: 7.82, fill: "#9c3d46" },
  { x: 462.09, y: 446.80, r: 16.66, fill: "#7e2937" },
  { x: 158.04, y: 457.54, r: 18.33, fill: "#872233" },
  { x: 285.57, y: 462.15, r: 22.22, fill: "#521222" },
  { x: 534.31, y: 448.02, r: 6.56, fill: "#7b3841" },
  { x: 414.18, y: 467.58, r: 19.40, fill: "#661929" },
  { x: 113.21, y: 476.53, r: 14.96, fill: "#aa3443" },
  { x: 505.41, y: 472.64, r: 9.86, fill: "#81353f" },
  { x: 72.15, y: 472.36, r: 8.40, fill: "#a23c46" },
  { x: 229.43, y: 484.77, r: 20.91, fill: "#651628" },
  { x: 361.72, y: 489.02, r: 20.81, fill: "#571324" },
  { x: 468.02, y: 492.58, r: 13.77, fill: "#7a2534" },
  { x: 527.12, y: 487.55, r: 4.73, fill: "#6f363c" },
  { x: 179.90, y: 504.76, r: 17.87, fill: "#8b2334" },
  { x: 306.73, y: 510.29, r: 20.66, fill: "#5a1426" },
  { x: 426.42, y: 513.36, r: 16.39, fill: "#6d1b2b" },
  { x: 85.96, y: 510.15, r: 7.94, fill: "#a03e47" },
  { x: 135.47, y: 522.03, r: 14.27, fill: "#a83442" },
  { x: 504.23, y: 515.85, r: 6.80, fill: "#793039" },
  { x: 254.21, y: 531.05, r: 19.25, fill: "#731a2c" },
  { x: 379.20, y: 534.26, r: 18.08, fill: "#661828" },
  { x: 469.13, y: 533.99, r: 9.49, fill: "#792935" },
  { x: 205.75, y: 547.46, r: 16.32, fill: "#9b2b3c" },
  { x: 105.07, y: 544.41, r: 8.00, fill: "#a03f48" },
  { x: 328.89, y: 553.96, r: 18.43, fill: "#6c1a2b" },
  { x: 432.25, y: 555.22, r: 12.43, fill: "#792231" },
  { x: 491.62, y: 548.60, r: 4.48, fill: "#6a3138" },
  { x: 161.65, y: 560.85, r: 12.86, fill: "#ab3b47" },
  { x: 278.66, y: 571.73, r: 16.93, fill: "#8d2436" },
  { x: 390.16, y: 574.94, r: 13.89, fill: "#812434" },
  { x: 461.92, y: 571.71, r: 5.76, fill: "#752f39" },
  { x: 129.32, y: 576.03, r: 7.17, fill: "#9e434b" },
  { x: 231.88, y: 585.66, r: 14.06, fill: "#a83645" },
  { x: 345.93, y: 592.70, r: 14.38, fill: "#8e2838" },
  { x: 190.60, y: 595.55, r: 10.85, fill: "#ad4651" },
  { x: 429.06, y: 593.72, r: 7.33, fill: "#832e3a" },
  { x: 300.73, y: 606.67, r: 13.15, fill: "#a13443" },
  { x: 155.85, y: 603.03, r: 5.47, fill: "#9e4c53" },
  { x: 396.31, y: 609.39, r: 9.35, fill: "#8f313e" },
  { x: 257.13, y: 617.26, r: 10.70, fill: "#aa454f" },
  { x: 361.29, y: 623.30, r: 9.97, fill: "#973441" },
  { x: 188.38, y: 623.33, r: 4.58, fill: "#a0565a" },
  { x: 222.48, y: 630.88, r: 6.87, fill: "#a44a52" },
  { x: 324.02, y: 633.21, r: 9.04, fill: "#a13f49" },
  { x: 286.27, y: 638.84, r: 6.48, fill: "#9e464f" },
];

const crownLeaves = [
  { d: "M 219.8 236.0 Q 183.8 171.5 110.8 160.9 Q 146.7 225.4 219.8 236.0 Z", fill: "#9a2942" },
  { d: "M 223.4 233.6 Q 233.9 155.5 185.9 93.0 Q 175.4 171.1 223.4 233.6 Z", fill: "#7a182f" },
  { d: "M 236.8 233.6 Q 270.6 166.1 244.0 95.5 Q 210.2 162.9 236.8 233.6 Z", fill: "#6c172b" },
  { d: "M 244.0 236.0 Q 292.1 194.9 287.6 131.8 Q 239.6 172.9 244.0 236.0 Z", fill: "#8e1f38" },
  { d: "M 218.6 243.3 Q 170.2 214.2 118.0 236.0 Q 166.5 265.0 218.6 243.3 Z", fill: "#a8324a" },
  { d: "M 251.3 242.0 Q 290.5 228.1 299.8 187.5 Q 260.6 201.5 251.3 242.0 Z", fill: "#a02d44" },
];
---

<div class="anara-anim relative w-full max-w-[26rem] md:max-w-[30rem] lg:max-w-[34rem] mx-auto">
  <svg
    viewBox="0 0 600 760"
    class="w-full h-auto block drop-shadow-[0_30px_50px_rgba(185,29,58,0.30)]"
    preserveAspectRatio="xMidYMid meet"
    role="img"
    aria-label="Anara pomegranate mark"
  >
    {/* Radiating crown of leaf petals */}
    <g data-leaves style="transition: opacity 800ms ease;">
      {crownLeaves.map((l) => <path d={l.d} fill={l.fill} />)}
    </g>

    {/* Edges of the knowledge graph — populated by JS during cluster phases */}
    <g data-edges fill="none" stroke="rgba(243,237,224,0.32)" stroke-width="0.65" style="opacity: 0; transition: opacity 700ms ease;"></g>

    {/* The dot field — the body of the fruit at rest, the swarm during motion */}
    <g data-dots>
      {dots.map((d) => (
        <circle
          cx={d.x} cy={d.y} r={d.r}
          fill={d.fill}
          data-hx={d.x} data-hy={d.y} data-hr={d.r}
        />
      ))}
    </g>

    {/* One-word service punch shown during cluster phases */}
    <text
      data-word
      x="300" y="706"
      text-anchor="middle"
      style="font-family: 'Cormorant Garamond', Georgia, serif; font-size: 64px; font-weight: 500; letter-spacing: -0.01em; fill: #f3ede0; opacity: 0; transition: opacity 700ms ease;"
    >
      Found.
    </text>
  </svg>
</div>

<script is:inline>
(function () {
  const wrap = document.querySelector(".anara-anim");
  if (!wrap) return;
  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

  const SVG_NS = "http://www.w3.org/2000/svg";
  const svg = wrap.querySelector("svg");
  const dotEls = Array.from(svg.querySelectorAll("[data-dots] circle"));
  const wordEl = svg.querySelector("[data-word]");
  const leavesEl = svg.querySelector("[data-leaves]");
  const edgesG = svg.querySelector("[data-edges]");

  // Centre the motion on the fruit's core.
  const CX = 300, CY = 412;

  // Spring-physics state per dot
  const dots = dotEls.map((el, i) => ({
    el, idx: i,
    hx: +el.dataset.hx, hy: +el.dataset.hy, hr: +el.dataset.hr,
    x: +el.dataset.hx, y: +el.dataset.hy, r: +el.dataset.hr,
    vx: 0, vy: 0,
    tx: +el.dataset.hx, ty: +el.dataset.hy, tr: +el.dataset.hr,
    seed: Math.random(),
    seed2: Math.random(),
    pulsePhase: Math.random() * Math.PI * 2,
    isAnchor: false,
  }));
  const N = dots.length;

  // The knowledge-graph skeleton: 6 anchor nodes in a hexagon around the centre.
  const ANCHOR_COUNT = 6;
  let anchorPositions = [];

  function computeAnchors(phaseIndex) {
    const baseRot = phaseIndex * 0.18 * Math.PI * 2;
    const radius = 145;
    return Array.from({ length: ANCHOR_COUNT }, (_, i) => {
      const a = (i / ANCHOR_COUNT) * Math.PI * 2 + baseRot;
      return {
        x: CX + Math.cos(a) * radius,
        y: CY + Math.sin(a) * radius * 0.78,
      };
    });
  }

  // --- Target generators ---
  function setHome() {
    dots.forEach((d) => { d.tx = d.hx; d.ty = d.hy; d.tr = d.hr; d.isAnchor = false; });
    edgesG.style.opacity = "0";
  }

  function setScatter() {
    dots.forEach((d) => {
      const angle = d.seed * Math.PI * 2;
      const dist = 130 + d.seed2 * 200;
      d.tx = CX + Math.cos(angle) * dist;
      d.ty = CY + Math.sin(angle) * dist;
      d.tr = 2 + d.seed * 4;
      d.isAnchor = false;
    });
    edgesG.style.opacity = "0";
  }

  function setKnowledgeCluster(phaseIndex) {
    anchorPositions = computeAnchors(phaseIndex);
    const groupSize = Math.floor(N / ANCHOR_COUNT);

    dots.forEach((d, i) => {
      const anchorIdx = Math.min(Math.floor(i / groupSize), ANCHOR_COUNT - 1);
      const anchor = anchorPositions[anchorIdx];
      const isFirstInGroup = (i % groupSize) === 0;

      if (isFirstInGroup) {
        d.tx = anchor.x;
        d.ty = anchor.y;
        d.tr = 12 + d.seed * 3;
        d.isAnchor = true;
      } else {
        const orbitAngle = d.seed * Math.PI * 2 + phaseIndex * 0.6;
        const orbitR = 16 + d.seed2 * 32;
        d.tx = anchor.x + Math.cos(orbitAngle) * orbitR;
        d.ty = anchor.y + Math.sin(orbitAngle) * orbitR;
        d.tr = 1.8 + d.seed * 3;
        d.isAnchor = false;
      }
    });

    edgesG.innerHTML = "";
    for (let i = 0; i < anchorPositions.length; i++) {
      const a = anchorPositions[i];
      const next = anchorPositions[(i + 1) % anchorPositions.length];
      const e1 = document.createElementNS(SVG_NS, "line");
      e1.setAttribute("x1", a.x.toFixed(2));
      e1.setAttribute("y1", a.y.toFixed(2));
      e1.setAttribute("x2", next.x.toFixed(2));
      e1.setAttribute("y2", next.y.toFixed(2));
      edgesG.appendChild(e1);
      const diag = anchorPositions[(i + 2) % anchorPositions.length];
      const e2 = document.createElementNS(SVG_NS, "line");
      e2.setAttribute("x1", a.x.toFixed(2));
      e2.setAttribute("y1", a.y.toFixed(2));
      e2.setAttribute("x2", diag.x.toFixed(2));
      e2.setAttribute("y2", diag.y.toFixed(2));
      e2.setAttribute("stroke-opacity", "0.5");
      edgesG.appendChild(e2);
    }
    edgesG.style.opacity = "1";
  }

  const setLeaves = (a) => { if (leavesEl) leavesEl.style.opacity = String(a); };

  // The cycle — pomegranate at rest, disperse, three clusters, reform.
  const sequence = [
    { name: "rest",     duration: 3800, action: setHome,                       word: null,      leaves: 1.0 },
    { name: "disperse", duration: 1200, action: setScatter,                    word: null,      leaves: 0.18 },
    { name: "found",    duration: 2500, action: () => setKnowledgeCluster(0),  word: "Found.",  leaves: 0.10 },
    { name: "filled",   duration: 2500, action: () => setKnowledgeCluster(1),  word: "Filled.", leaves: 0.10 },
    { name: "kept",     duration: 2500, action: () => setKnowledgeCluster(2),  word: "Kept.",   leaves: 0.10 },
    { name: "reform",   duration: 1800, action: setHome,                       word: null,      leaves: 1.0 },
  ];

  let idx = 0;
  let phaseStart = performance.now();

  function applyPhase(p) {
    p.action();
    if (p.word) {
      wordEl.textContent = p.word;
      wordEl.style.opacity = "1";
    } else {
      wordEl.style.opacity = "0";
    }
    setLeaves(p.leaves);
  }
  applyPhase(sequence[0]);

  const STIFFNESS = 0.028;
  const DAMPING = 0.83;

  let raf = 0;
  let running = false;
  const startTime = performance.now();

  function tick(now) {
    if (!running) return;
    if (now - phaseStart > sequence[idx].duration) {
      idx = (idx + 1) % sequence.length;
      phaseStart = now;
      applyPhase(sequence[idx]);
    }

    const isRest = sequence[idx].name === "rest";
    const t = (now - startTime) * 0.001;

    for (let i = 0; i < dots.length; i++) {
      const d = dots[i];

      let tx = d.tx, ty = d.ty;
      if (isRest) {
        tx += Math.sin(t * 1.2 + d.seed * 6) * 0.6;
        ty += Math.cos(t * 1.5 + d.seed2 * 5) * 0.5;
      }

      d.vx = (d.vx + (tx - d.x) * STIFFNESS) * DAMPING;
      d.vy = (d.vy + (ty - d.y) * STIFFNESS) * DAMPING;
      d.x += d.vx;
      d.y += d.vy;

      d.r += (d.tr - d.r) * 0.09;

      let displayR = d.r;
      if (d.isAnchor) {
        displayR = d.r * (1 + Math.sin(t * 3.2 + d.pulsePhase) * 0.14);
      }

      d.el.setAttribute("cx", d.x.toFixed(2));
      d.el.setAttribute("cy", d.y.toFixed(2));
      d.el.setAttribute("r", displayR.toFixed(2));
    }

    raf = requestAnimationFrame(tick);
  }

  function start() {
    if (running) return;
    running = true;
    phaseStart = performance.now();
    raf = requestAnimationFrame(tick);
  }
  function stop() {
    if (!running) return;
    running = false;
    cancelAnimationFrame(raf);
  }

  const io = new IntersectionObserver(
    (entries) => entries.forEach((e) => (e.isIntersecting ? start() : stop())),
    { threshold: 0.05 }
  );
  io.observe(wrap);
})();
</script>

